#include <stdio.h>

// 简单的加法函数
int add(int a, int b) {
    return a + b;
}

// 简单的乘法函数
int multiply(int a, int b) {
    return a * b;
}

int main() {
    printf("Hello, CI/CD World!\n");
    printf("3 + 4 = %d\n", add(3, 4));
    printf("3 * 4 = %d\n", multiply(3, 4));
    return 0;
}

